<?php

// config/local.php.example

/**
 * This is an example configuration file for local, sensitive data.
 * To use it, copy this file to `config/local.php` and fill in the actual values.
 * The `config/local.php` file is ignored by Git to keep your credentials secure.
 */

return [
    'database' => [
        'host' => '127.0.0.1',
        'port' => '3306',
        'database' => 'your_db_name',
        'username' => 'your_db_user',
        'password' => 'your_db_password',
    ],

    // You can add other sensitive data here in the future
    // 'api_keys' => [
    //     'some_service' => 'your_api_key'
    // ]
];
