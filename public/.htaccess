<IfModule mod_rewrite.c>
    RewriteEngine On

    # Arahkan semua permintaan yang bukan file atau direktori ke index.php
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
